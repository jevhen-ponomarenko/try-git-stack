######### Creating two depndant branches###########

git switch -c feature1;
touch new-feature.txt;
echo "such cool wow" > new-feature.txt;
git add -A; git commit -m "new feature";
git-stack --push;

echo "Make a PR for the first branch manually"

git switch -c feature2;
touch extending-new-feature.txt;
echo "OMG this is even more cooler" > extending-new-feature.txt;
git add -A; git commit -m "more work on new feature";
git-stack --push;

git-stack;


######### Some fixes on feature1 ###########
git checkout feature1;
echo "Ohh I forgot this" >> new-feature.txt;
git add . ;
git log --oneline;
git commit --fixup 87293b0;









